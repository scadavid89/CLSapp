# Copy to .env for local development. .env is gitignored — never commit real values.
# Production values live in Azure app settings or Key Vault, not in this repo.

VITE_APP_ENV=local

# --- API (added in Phase 2, when the prototype gets a backend) ---
# VITE_API_BASE=http://localhost:7071/api

# --- Server-side only. These must NEVER carry a VITE_ prefix: anything
# --- prefixed VITE_ is inlined into the browser bundle and is public.
# QBO_CLIENT_ID=
# QBO_CLIENT_SECRET=
# QBO_REALM_ID=
# SQL_CONNECTION_STRING=
